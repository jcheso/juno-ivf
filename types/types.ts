export type PhoneNumber = {
  countryCode: string
  number: string
}

export type Address = {
  flatNumber: string
  buildingNumber: string
  street: string
  city: string
  state: string
  postCode: string
}
